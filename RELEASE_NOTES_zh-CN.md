# OpenFrequency 3.1 发布说明

## 2026-04-05 更新

> **发布日期**: 2026-04-05
> **版本**: **3.1**

本次更新重点围绕模拟器接入、机场频率数据、ATIS 真实性和仪表盘交互体验展开，同时修复了大量多语言、频道切换、地图显示与音频播放方面的问题。

## 新功能

- **支持 X-Plane Local Web API**：移除了旧的 `XPlaneConnect` 路径，改为接入 X-Plane 12 官方 Local Web API。
- **机场频率服务**：新增对 `airports.csv`、`airport-frequencies.csv`、`runways.csv` 的本地缓存与查询支持。
- **附近机场面板**：原“雷达（交通）”标签改为“附近机场与频率”，可直接点击调频。
- **按频率保存上下文**：每个频率拥有独立的 ATC 聊天上下文，切回原频道时可恢复历史。
- **前端模拟器切换**：设置页可直接切换模拟器类型并填写 X-Plane 主机与端口，无需手改 JSON。
- **航路设置增强**：设置页新增起飞机场、目的地、备降场、巡航高度和航路字段，并支持 SimBrief 回填。
- **SimBrief 呼号自动导入**：可从 SimBrief 自动提取呼号并写回运行时状态与配置文件。

## ATIS 改进

- **完整的 ATIS 播放链路**：切换到 ATIS 后会正确触发 METAR 获取、ATIS 生成、TTS 播报和通讯记录写入。
- **支持重复播报**：重新切回同一 ATIS 频率时会再次播报，而不是只播一次。
- **通讯记录写入 ATIS 正文**：ATIS 全文现在会进入通讯记录，不再只是显示系统提示。
- **真实观测时间**：优先使用 AviationWeather.gov 返回的 `reportTime` / `obsTime`，不再使用占位文字。
- **使用跑道播报**：接入 `runways.csv`，可根据风向推断并播报使用跑道。
- **中国机场双语 ATIS**：对于中国机场，无论界面语言是什么，都会生成中英双语 ATIS。
- **措辞优化**：中文 ATIS 调整为更接近中国民航通播风格，英文 ATIS 也收紧到更接近实际无线电表述。
- **显示与朗读分离**：界面显示普通数字，TTS 则按航空话音规则朗读。

## TTS 与语言

- **英语数字话音规则**：英语 TTS 使用 `Tree`、`Fower`、`Fife`、`Niner` 等航空数字读法。
- **数字展开规则**：支持逐位读数，并支持 `Hundred`、`Thousand`、`Decimal` 等模式。
- **日语语音行为**：日语交互模式下使用日语声线，并将英文式内容转换为片假名发音，但界面显示文本保持不变。
- **中文航空读法**：中文 TTS 可对能见度、云高、QNH、风等内容使用更接近航行情景的读法。

## 界面与体验

- **机组频道修复**：机组模式下发送的文本不再误发给 ATC，前后端都已加保护。
- **机组按钮高亮修复**：机组频道选中时现在会正确高亮显示。
- **多语言补全**：修复了一批动态状态文本缺失中日文翻译的问题。
- **深色模式修复**：设置页中的辅助文本、标签和提示在深色模式下可正常阅读。
- **地图高度渐变**：飞行轨迹改为连续高度渐变显示，图例更准确。
- **轨迹持久化恢复**：页面刷新后可更完整地恢复飞行轨迹。
- **顺序音频播放**：浏览器端改为队列播放，避免 ATIS 或无线电音频重叠。

## 模拟器与故障逻辑

- **按模拟器能力限制故障**：随机故障仅在支持注入的模拟器上触发。
- **X-Plane 主链路接入**：`SimBridge` 现在会按配置使用对应模拟器后端，而不是始终按 MSFS/SimConnect 处理。
- **诊断信息增强**：补充了更清晰的 X-Plane 和 ATIS 调试信息。

## 当前限制

- **跑道选择仍为启发式推断**：当前“使用跑道”基于风向和跑道数据推断，不是实时运行配置。
- **ATIS 措辞仍在持续收紧**：中英文短语已有明显改进，但还不是完整的真实台站级措辞引擎。
- **X-Plane 依赖官方 Web API**：X-Plane 支持需要启用并连通官方 Local Web API。

---

英文版本请参阅 [RELEASE_NOTES.md](./RELEASE_NOTES.md)。
