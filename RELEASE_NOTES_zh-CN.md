# OpenFrequency 发布说明

---

# v3.9-beta — 2026-04-29

> **发布日期**：2026-04-29
> **版本**：**v3.9-beta**
> **状态**：**Alpha**

本版本是一次重大功能里程碑，引入了 CPDLC 数字放行通信、中国空域米制 RVSM 自动切换、完整 MSI 安装程序构建流程、社区插件支持、雷达引导、通过 Cloudflare Workers 自动更新、崩溃遥测上报、双层 LLM 配置、本地流式 TTS，以及 v3.9-beta 的数十项错误修复。

英文版本请参阅 [RELEASE_NOTES.md](./RELEASE_NOTES.md)。

---

## 错误修复

| 模块 | 修复内容 |
|------|---------|
| 网络 | 局域网设备无法访问仪表盘 |
| 生涯模式 | 选择生涯任务后进行自由飞行，呼号会被锁定在任务中 |
| 编译打包 | 部分用户数据目录在编译版本中找不到 |
| ATIS | Information 字母每次都是 Alpha |
| ATIS | 部分机场名称会直接朗读 ICAO 代码而非本地名称 |
| ATIS | ATIS 不会反复播报（现在重新调台后若内容未变则只在通话记录重新展示，不重新朗读） |
| 导航 | 优化 VFR 引导功能 |
| 编译打包 | 编译版本语音转文字模块启动时会闪出命令行窗口 |
| 地面寻路 | 机场自动寻路功能存在但无法从 UI 访问 |
| 仪表盘 | 飞机升空后不会自动展示 SimBrief 航路预测 |
| ATC | 优化 ATC 主动发话功能 |
| 音频 | 优化无线电噪声效果 |
| ATC | 优化 ATC 不知道真实频率信息的问题 |
| X-Plane | 修复 X-Plane 12 频率注入格式不正确的问题 |
| 仪器 | 修复垂直速度（V/S）计算错误 |
| X-Plane | 修复 X-Plane 12 LiveTraffic AI 飞机状态无法读取 |

---

## 新功能

### MSI 安装程序
- WiX 4 MSI 构建流程，通过 `installer/OpenFrequency.wxs` 和 `installer/build_installer.ps1` 完成。
- 全机安装，包含开始菜单和桌面快捷方式、大版本升级支持及"程序和功能"条目。
- `version.txt` 为版本号唯一来源。

### CPDLC
- 支持管制员-飞行员数据链通信（CPDLC）。
- 处理起飞前放行、洋区及航路数据链消息。

### 中国空域米制 RVSM
- 在中国 RVSM 空域自动切换为米制高度（米）。
- ATC 通话和放行单中使用正确的公制飞行高度层。

### 快速回复模板
- 大部分常用 ATC 复诵已预设模板，减少输入时间并规范用语。

### 双层 LLM 模型支持
- 可分别配置轻量模型（快速、低成本）和思考模型（较慢、质量更高）。
- 常规复诵使用轻量模型，复杂放行及模糊情境升级至思考模型处理。

### 本地流式 TTS
- 支持在 Edge-TTS（云端）和本地流式文字转语音模型之间切换。
- 本地模型文件存放于 `%APPDATA%\OpenFrequency\models`。

### 雷达引导
- 仪表盘新增 ATC 雷达引导面板。
- 支持 HDG / ALT / SPD 引导指令输入，实时派发至 ATC 逻辑引擎。

### 插件系统
- `OpenFrequencyPlugin` 基类，提供生命周期钩子（`on_load`、`on_unload`、`on_event`、`on_config_update`）。
- 插件管理器从 `plugins/` 目录自动发现并加载插件。
- 内置 DLC 目录浏览器，一键下载 FlyByWire A32NX 至 MSFS。

### 崩溃遥测与反馈
- 未捕获异常经 PII 脱敏后（需用户同意）上报至 Cloudflare Workers。
- 设置页支持手动上报崩溃日志及提交用户反馈（可附带备注）。
- 上报接口为 Workers 的 `/api/crash` 和 `/api/feedback`。

### 通过 Cloudflare Workers 自动更新
- 版本检查限制为每天一次。
- 版本元数据和安装包通过 Workers 代理，加快国内下载速度。
- 下载完成后验证 SHA-256 校验值，再启动安装程序。

### 非英语 ATC 开关
- 新增"允许国际航班非英语 ATC 通话"开关，适用于非中文国际机场。
- 默认关闭，标注为"不符合真实运行规范"。

### 自动忙碌/待机
- 开启"自动忙碌等级"后，根据附近飞机实时数量自动调整工作负载等级。
- 自动忙碌开启时隐藏交通密度下拉框。

### SimConnect SDK 自动打包
- PyPI `SimConnect` 包内置的 `SimConnect.dll` 在打包时自动收集，无需手动下载。

---

# v3.9-beta — 2026-04-18

> **发布日期**：2026-04-18
> **版本**：**v3.9-beta**
> **状态**：**Beta**

本版本带来了云端崩溃上报与自动检测更新基础设施、国际非英语 ATC 通话开关、流量自适应自动忙碌等级管理、SimConnect SDK 自动打包、雷达引导面板，以及 WiX 4 MSI 安装程序构建流程。

### 云服务与自动更新

- **崩溃上报**：未捕获异常和线程崩溃会被静默捕获，经 PII 脱敏后（经用户同意）上传至 OpenFrequency 云端。可在设置 → 隐私与更新中关闭。
- **手动日志上报**：在设置页点击"上报近期崩溃"，可随时手动发送经脱敏处理的崩溃报告或日志摘要。
- **自动检测更新**：OpenFrequency 每天自动检测一次新版本，也可在设置页手动触发。
- **应用内下载**：新版本直接在应用内下载并显示进度；启动安装程序前会验证 SHA-256 校验值。
- **国内下载加速**：版本元数据和安装包均通过 OpenFrequency Cloudflare Workers 节点代理，改善国内下载速度。
- **反馈表单**：设置页内置反馈表单，无需离开应用即可提交 Bug 报告或建议。
- **隐私保护**：API 密钥、Bearer Token、Windows 用户路径和邮箱地址均会从上报内容中过滤。

### 国际 ATC 语言支持

- 设置页新增"允许国际航班非英语 ATC 通话"开关，默认关闭，标注为"不符合实际运行规范"。
- 当 STT 语言设为日语且开关打开时，ATC 和机组通信使用日语语气。

### 流量自适应自动忙碌/待机

- 开启"自动忙碌等级"后，根据附近飞机数量自动切换工作负载等级。
- 设置页实时显示当前忙碌等级和飞机数量。

### SimConnect SDK 自动打包

- `SimConnect.dll` 在打包时自动收集，无需手动配置。

### 雷达引导面板

- 仪表盘新增雷达引导面板，支持 HDG、ALT、SPD 引导输入。

### 插件系统

- 插件 API、插件管理器和 Addon 安装器。

### MSI 安装程序流程

- WiX 4 MSI，`version.txt` 为版本号唯一来源。

---

# v3.1 — 2026-04-05

X-Plane Local Web API 支持、机场频率服务、ATIS 双语支持、中文 TTS 航空用语等。

---

# v2.5 / v2.0 Alpha — 2026-02-08

首次公开 Alpha，引入生涯模式、机组通信重构、紧急事件系统 2.0 和多语言支持。
