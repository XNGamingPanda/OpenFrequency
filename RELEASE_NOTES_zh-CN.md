# OpenFrequency v3.9-beta 发布说明

## 2026-04-18 更新

> **发布日期**：2026-04-18
> **版本**：**v3.9-beta**
> **状态**：**Beta**

本版本带来了云端崩溃上报与自动检测更新基础设施、国际非英语 ATC 通话开关、流量自适应自动忙碌等级管理、SimConnect SDK 自动打包、雷达引导面板，以及 WiX 4 MSI 安装程序构建流程。

### 云服务与自动更新

- **崩溃上报**：未捕获异常和线程崩溃会被静默捕获，经 PII 脱敏后（经用户同意）上传至 OpenFrequency 云端。可在设置 → 隐私与更新中关闭。
- **手动日志上报**：在设置页点击"上报近期崩溃"，可随时手动发送经脱敏处理的崩溃报告或日志摘要。
- **自动检测更新**：OpenFrequency 启动 8 秒后自动检测新版本，也可在设置页手动触发。
- **应用内下载**：新版本直接在应用内下载并显示进度；启动安装程序前会验证 SHA-256 校验值。
- **国内下载加速**：版本元数据和安装包均通过 OpenFrequency Cloudflare Workers 节点代理，改善国内下载速度。
- **反馈表单**：设置页内置反馈表单，无需离开应用即可提交 Bug 报告或建议。
- **隐私保护**：API 密钥、Bearer Token、Windows 用户路径和邮箱地址均会从上报内容中过滤。仅在开启"包含模拟器信息"时，上报内容才包含飞机、机场、飞行阶段等信息。

### 国际 ATC 语言支持

- **非英语 ATC 开关**：设置页新增"允许国际航班非英语 ATC 通话"开关，开启后 AI 会在非中文国际机场使用本地语言通话。默认关闭，标注为"不符合实际运行规范"。
- **日语通话路径**：当 STT 语言设为日语且开关打开时，ATC 和机组通信使用日语语气。
- **ICAO 英语默认保持不变**：开关关闭时，国际航班继续使用标准 ICAO 英语通话规范。

### 流量自适应自动忙碌/待机

- **自动忙碌等级**：开启"自动忙碌等级"后，工作负载模拟器会根据附近飞机数量（0 / 1–3 / 4–10 / 11+）自动切换 `静默 / 低 / 中 / 高` 等级。
- **仪表盘实时指示**：设置页实时显示当前有效忙碌等级和飞机数量，通过 Socket.IO 推送更新。
- **流量门槛**：待机概率和忽略概率现在与流量密度挂钩，在空域非常安静时减少不必要的 ATC 响应。

### SimConnect SDK 自动打包

- **自动包含 DLL**：PyPI `SimConnect` 包内置的 `SimConnect.dll` 在打包时会被自动收集并捆绑进发布包。无需手动下载或配置 PATH。
- **优雅降级**：如果构建环境中未安装 `SimConnect`，打包仍可成功完成，仅在运行时该连接器不可用。

### 雷达引导面板

- **全新 📡 雷达跟随面板**：仪表盘新增雷达引导面板，通过开关启用雷达引导模式。
- **手动引导输入**：HDG、ALT、SPD 三个输入框各带一个 ▶ 发送按钮，支持按 Enter 发送。
- **Socket 事件**：`set_radar_vector_mode` 和 `manual_radar_vector` 事件分发至后端与 ATC 逻辑引擎集成。

### 插件系统

- **插件 API**：`OpenFrequencyPlugin` 基类提供生命周期钩子（`on_load`、`on_unload`、`on_event`、`on_config_update`）。
- **插件管理器**：从 `plugins/` 目录发现并动态加载插件，支持 manifest 元数据。
- **Addon 安装器**：可从插件页一键安装 DLC / FlyByWire A32NX 等扩展内容。

### MSI 安装程序流程

- **WiX 4 MSI**：`installer/OpenFrequency.wxs` 定义了完整的 per-machine MSI，包含开始菜单和桌面快捷方式、大版本升级支持以及"程序和功能"条目。
- **构建脚本**：`installer/build_installer.ps1` 读取 `version.txt`，运行 PyInstaller，最终生成 `dist/OpenFrequency-{version}-Setup.msi`，一条命令完成全部流程。
- **版本注入**：构建脚本通过 `-d` 参数将 `$(env.OF_VERSION)` 传递给 WiX，`version.txt` 是版本号的唯一来源。

英文版本请参阅 [RELEASE_NOTES.md](./RELEASE_NOTES.md)。

---

# OpenFrequency v3.5-beta 发布说明

## 2026-04-07 更新

> **发布日期**：2026-04-07  
> **版本**：**v3.5-beta**  
> **状态**：**Beta**。相比 3.1 alpha，这个版本已经更完整，但生涯平衡、地面数据质量和部分模拟器原生解析仍在持续调整。

本次更新是一次较大的 Beta 里程碑，重点是降低部署门槛、重做生涯模式流程、强化 SimBrief 航路生成与导入、改进 X-Plane 和机场地面数据接入，并让 ATC 更主动地理解飞行状态。

## 桌面打包

- **Windows EXE 打包**：保留现有 Flask / Socket.IO / HTML 架构，新增 PyInstaller 打包路径。
- **桌面启动器**：启动 EXE 后自动拉起本地 Web 服务，并打开仪表盘。
- **隐藏终端运行**：普通打包版运行时不再显示终端窗口。
- **调试版控制台**：保留 `OpenFrequency-Console.exe`，用于排查启动和依赖问题。
- **系统托盘控制**：新增托盘图标，可打开仪表盘、打开日志目录、退出 OpenFrequency。
- **运行日志目录**：打包版日志统一写入 `%APPDATA%\OpenFrequency\logs`。
- **配置文件处理**：打包时不包含本机 `config.json`，首次运行从 `config.example.json` 复制到用户数据目录。
- **资源过滤**：打包时排除临时文件、日志、用户生涯档案、调试音频和本地错误文件，同时保留模型、模板、静态资源和必要数据。

## 生涯模式重做

- **修复任务数量显示**：修复“可用任务”重复显示的问题。
- **接受任务后自动进入仪表盘**：点击接受任务后会进入类似自由飞行的 Dashboard 流程。
- **飞行员昵称**：`STUDENT01` 这类显示名现在是昵称，可在生涯页修改，不再和任务呼号混在一起。
- **航空公司签约**：接任务前需要先和地区内航空公司签约，后续任务使用该航空公司的呼号。
- **地区航司适配**：任务生成会根据当前机场地区选择更符合当地环境的航空公司。
- **客货运一致性**：`FDX / UPS` 等货运公司只生成货运任务，客运公司默认不生成纯货运任务。
- **已安装机型发现**：任务机型会优先来自模拟器已安装飞机，而不是只从固定列表中选择。
- **执照等级限制**：任务机型会根据当前证书等级筛选，低等级不会直接刷出不适配的重型机。
- **飞行准备检查**：开始生涯飞行前会检查始发机场、是否在地面、是否停稳、机型是否匹配、是否冷舱。
- **重复进入也会提示**：第二次进入生涯 Dashboard 时，如果机型或位置仍不正确，仍会显示准备提示。
- **生涯呼号锁定**：有 active job 时，运行时呼号锁定为任务呼号，不会被设置页或 SimBrief 导入覆盖。

## SimBrief 与航路

- **任务卡 SimBrief 链接**：任务卡和“当前任务”面板都提供 SimBrief Dispatch 链接。
- **自动带入参数**：SimBrief URL 会带入起飞机场、目的地、机型、航司、航班号、呼号、任务类型和 IFR 规则。
- **导入引导**：Dashboard 的生涯准备提示会引导用户先生成 SimBrief 航路，再使用设置页保存的 SimBrief 用户名导入 OFP。
- **旧任务兼容**：旧 active job 如果没有 `simbrief_url`，后端会动态补出链接。
- **航路回退**：未导入 SimBrief OFP 前，内部 flight plan 暂时使用 `DIRECT`。

## X-Plane 与模拟器接入

- **X-Plane 官方 Local Web API**：继续使用 X-Plane 12 官方 Local Web API，不再依赖 XPlaneConnect。
- **当前机型识别**：X-Plane 通过 aircraft dataref 读取当前机型信息，MSFS 通过 SimConnect 读取标题/模型信息。
- **COM 频率同步**：在模拟器中调频可以同步更新 OpenFrequency 的 ATC 上下文，并避免和网页调频互相打架。
- **连接状态刷新**：Dashboard 和移动端会定期刷新模拟器连接状态，避免断开后 UI 仍显示已连接。
- **故障注入调低**：随机故障率降低，低档不再过于频繁；X-Plane 故障只有写入成功后才会提示和通知 ATC。

## 机场数据、地面寻路与 EFB 能力

- **X-Plane 原生地面数据**：X-Plane 下优先读取 `apt.dat`，不再把 Little Navmap 当作主要机场地面数据源。
- **OpenStreetMap 地面数据**：新增 OSM / Overpass 支持，可获取滑行道、停机坪、跑道和机位点。
- **数据源设置**：设置页可切换频率数据和地面布局数据来源，例如模拟器原生或第三方数据。
- **加权滑行寻路**：机场地面被抽象成图，并在代价函数中考虑距离、跑道穿越、热点、转弯、复杂节点、低能见度和大机型限制。
- **地面 ATC 知道寻路结果**：Ground 频道会把地面布局摘要和建议滑行路线提供给 LLM，减少编造滑行道名。
- **最新指令卡片**：新增类似 BeyondATC 的指令提取卡片，可显示高度、航向、速度、QNH/Altimeter、进近、频率、应答机和滑行路线。
- **滑行道高亮**：当 ATC 给出滑行路线时，地图会高亮匹配的滑行道线段。
- **适配导航视野**：新增地图按钮，可自动缩放到高亮路径、航迹或当前位置。

## ATC、机组、客舱与音频

- **ATC 主动监控**：新增本地规则优先的 ATCMonitor，跟踪高度、航向、速度、移交、保持短停和许可相关偏差，再由 AI 判断是否需要开口。
- **切频停止音频**：切换频率时会停止上一段 ATC/ATIS/客舱媒体音频，并减少重复 `SYSTEM: Tuned`。
- **机组频道修复**：切到机组后，文本和语音都会走机组路径，不再误发给 ATC。
- **机组声音区分**：副驾驶和乘务长可使用不同 TTS 声线。
- **机组主动场景**：乘务长/副驾驶可以在客舱准备、爬升、服务、下降准备和落地后主动发起对话。
- **客舱媒体支持**：客舱广播可配置本地音频或视频；视频会在前端小窗播放。
- **ANA 模板支持**：可配置 ANA 安全视频、下机视频等客舱媒体。

## 当前限制

- **MSFS BGL 原生解析仍有限**：目前支持扫描 MSFS 包和读取可解析 XML，编译后 BGL 仍未完整反编译；缺失时回退到 OSM。
- **OSM 数据质量依赖机场**：部分机场 OSM 滑行道/机位数据可能不完整，地图高亮和寻路属于尽力而为。
- **媒体版权需用户自行负责**：客舱视频/音频需要用户确保合法来源和使用权。
- **生涯经济仍在调参**：报酬、XP、执照进阶和航司任务池仍属于 Beta 调整范围。

英文版本请参阅 [RELEASE_NOTES.md](./RELEASE_NOTES.md)。
